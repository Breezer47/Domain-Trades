<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Domain Trades | The Elite Circle</title>
    
    <!-- IBM Plex Sans Thai (Loopless) & Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Thai:wght@300;400;500;600;700&family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --bg-ivory: #FAF9F6;
            --gold-light: #D4AF37;
            --gold-main: #B89655;
            --gold-dark: #8A6D3B;
            --dark: #1A1A1A;
            --discord-blue: #5865F2;
            --font-main: 'IBM Plex Sans Thai', 'Inter', sans-serif;
            /* Gradient สำหรับตัวหนังสือ */
            --gold-gradient: linear-gradient(135deg, #B89655 0%, #E8D092 50%, #B89655 100%);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: var(--font-main);
            background-color: var(--bg-ivory);
            color: var(--dark);
            overflow-x: hidden;
            scroll-behavior: smooth;
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }

        /* --- Advanced Background Layer --- */
        .chart-bg {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            z-index: -1; pointer-events: none;
            perspective: 1000px;
        }

        .bg-element { position: absolute; will-change: transform; }

        /* เส้นกราฟจางๆ */
        .main-trend {
            width: 140%; top: 30%; left: -20%;
            opacity: 0.15;
        }

        /* เส้นระดับ Level */
        .lvl-line {
            width: 100%; height: 1px;
            border-top: 1px dashed var(--gold-main); opacity: 0.08;
        }

        /* แท่งเทียนกระจายตัว (หลายเลเยอร์) */
        .candle {
            display: flex; flex-direction: column; align-items: center;
            opacity: 0.12;
        }
        .wick { width: 1px; background: var(--gold-main); }
        .body-candle { width: 8px; background: var(--gold-main); border-radius: 1px; }

        /* --- Typography & Gradient Effect --- */
        .gradient-text {
            background: var(--gold-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            display: inline-block;
            font-weight: 700;
        }

        .container {
            max-width: 1100px; margin: 0 auto; padding: 0 20px; text-align: center;
        }

        header { padding-top: 100px; margin-bottom: 50px; }
        .logo {
            font-size: 0.9rem; letter-spacing: 8px;
            text-transform: uppercase; font-weight: 700;
            color: var(--gold-main); opacity: 0.8;
        }

        h1 {
            font-size: clamp(2.8rem, 8vw, 4.8rem);
            line-height: 1.1; margin-bottom: 25px;
            font-weight: 700; letter-spacing: -1.5px;
        }

        .subtext {
            font-size: clamp(1.2rem, 3vw, 1.7rem);
            color: #666; max-width: 850px;
            margin: 0 auto 70px; font-weight: 400;
        }

        /* --- Video Wrapper (Premium Border) --- */
        .video-wrapper {
            background: #fff; padding: 12px;
            border-radius: 40px; box-shadow: 0 40px 100px rgba(0,0,0,0.04);
            max-width: 950px; margin: 0 auto 100px;
            border: 1px solid rgba(0,0,0,0.02);
            position: relative;
        }
        /* แสง Glow จางๆ หลังวิดีโอ */
        .video-wrapper::after {
            content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0;
            background: radial-gradient(circle, rgba(184, 150, 85, 0.1) 0%, transparent 70%);
            z-index: -1; transform: scale(1.2);
        }

        .video-main {
            width: 100%; aspect-ratio: 16 / 9; background: #F4F3F0;
            border-radius: 30px; overflow: hidden; display: flex;
            align-items: center; justify-content: center;
        }
        .video-main video { width: 100%; height: 100%; object-fit: cover; }

        /* --- CTA Box --- */
        .cta-box {
            max-width: 750px; margin: 0 auto 150px;
            padding: 60px 40px; background: rgba(255,255,255,0.6);
            backdrop-filter: blur(15px); border-radius: 40px;
            border: 1px solid rgba(184, 150, 85, 0.15);
            box-shadow: 0 20px 50px rgba(0,0,0,0.03);
        }

        .btn-gold {
            display: block; width: 100%; max-width: 500px; margin: 0 auto 20px;
            background: linear-gradient(135deg, var(--gold-main) 0%, var(--gold-dark) 100%);
            color: white; padding: 24px; border-radius: 18px;
            text-decoration: none; font-weight: 700; font-size: 1.4rem;
            box-shadow: 0 15px 40px rgba(184, 150, 85, 0.25);
            transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        }
        .btn-gold:hover { transform: translateY(-6px); box-shadow: 0 25px 55px rgba(184, 150, 85, 0.35); }

        .divider { margin: 30px 0; font-size: 0.85rem; color: #BBB; letter-spacing: 3px; text-transform: uppercase; }

        .btn-discord {
            display: block; width: 100%; max-width: 500px; margin: 0 auto;
            background-color: var(--discord-blue); color: white;
            padding: 22px; border-radius: 18px; text-decoration: none;
            font-weight: 600; font-size: 1.2rem; transition: all 0.3s;
        }

        /* --- FAQ Area --- */
        .faq-section { padding-bottom: 150px; }
        .faq-title { font-size: clamp(2rem, 5vw, 3rem); margin-bottom: 80px; font-weight: 700; }

        .faq-item {
            max-width: 900px; margin: 0 auto 15px; background: #fff;
            border-radius: 20px; border: 1px solid rgba(0,0,0,0.04);
            transition: all 0.4s ease;
        }
        .faq-header { padding: 28px 35px; display: flex; align-items: center; cursor: pointer; }
        .faq-number {
            font-size: 0.75rem; font-weight: 700; color: var(--gold-main);
            border: 1px solid rgba(184, 150, 85, 0.3); padding: 4px 10px; border-radius: 6px; margin-right: 25px;
        }
        .faq-question { font-size: 1.3rem; color: var(--dark); font-weight: 600; flex-grow: 1; text-align: left; }
        .faq-indicator { display: flex; align-items: center; gap: 12px; color: var(--gold-main); }
        .arrow-svg { width: 22px; height: 22px; transition: transform 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55); }

        .faq-item.active { border-color: var(--gold-main); transform: scale(1.01); box-shadow: 0 20px 60px rgba(184, 150, 85, 0.08); }
        .faq-item.active .arrow-svg { transform: rotate(180deg); }

        .faq-content {
            max-height: 0; overflow: hidden;
            transition: max-height 0.7s cubic-bezier(0.4, 0, 0.2, 1);
            padding: 0 35px;
        }
        .faq-item.active .faq-content { max-height: 850px; padding-bottom: 35px; }

        .faq-video-inner { background: var(--bg-ivory); padding: 10px; border-radius: 24px; }
        .video-box { width: 100%; aspect-ratio: 16 / 9; background: #E5E4E0; border-radius: 18px; overflow: hidden; }
        .video-box video { width: 100%; height: 100%; object-fit: cover; }
    </style>
</head>
<body>

    <!-- Advanced Layered Background -->
    <div class="chart-bg" id="bg-master">
        <!-- Layer 1: Trend Line (Moves Slowest) -->
        <svg class="bg-element main-trend" id="trend-layer" viewBox="0 0 1000 200">
            <path d="M0,180 L150,165 L300,175 L450,110 L600,125 L800,45 L1000,55" fill="none" stroke="#B89655" stroke-width="1" />
        </svg>

        <!-- Layer 2: Key Levels (Static) -->
        <div class="bg-element lvl-line" style="top: 20%;"></div>
        <div class="bg-element lvl-line" style="top: 75%;"></div>

        <!-- Layer 3: Candles (Move Faster) -->
        <div class="bg-element candle" style="top: 15%; left: 10%;" data-depth="0.2">
            <div class="wick" style="height: 60px;"></div>
            <div class="body-candle" style="height: 25px; position: absolute; top: 15px;"></div>
        </div>
        <div class="bg-element candle" style="top: 40%; right: 12%;" data-depth="0.4">
            <div class="wick" style="height: 80px;"></div>
            <div class="body-candle" style="height: 35px; position: absolute; top: 20px;"></div>
        </div>
        <div class="bg-element candle" style="bottom: 25%; left: 15%;" data-depth="0.3">
            <div class="wick" style="height: 100px;"></div>
            <div class="body-candle" style="height: 45px; position: absolute; top: 25px;"></div>
        </div>
    </div>

    <div class="container">
        <header><div class="logo">Domain Trades</div></header>

        <!-- Headline with Gradient -->
        <h1>ทำความรู้จักกับ <span class="gradient-text">Domain Trades</span> ให้มากขึ้น</h1>
        
        <p class="subtext">
            สิ่งที่คุณจะได้รับจากการเข้าร่วม Domain Trades
        </p>

        <!-- Video Frame -->
        <div class="video-wrapper" id="main-video-frame">
            <div class="video-main">
                <video controls><source src="intro.mp4" type="video/mp4"></video>
            </div>
        </div>

        <h2 style="font-size: 2rem; font-weight: 600; margin-bottom: 50px;">
            มาเป็นส่วนหนึ่งของ<span class="gradient-text">การเดินทาง</span>
        </h2>

        <!-- CTA Section -->
        <div class="cta-box">
            <a href="https://lin.ee/RvPZdhK" target="_blank" rel="noopener noreferrer" class="btn-gold">
                ติดต่อผมที่นี่
            </a>
            <div class="divider">or</div>
            <a href="https://discord.com/invite/tx6VaWxXN" target="_blank" rel="noopener noreferrer" class="btn-discord">
                เข้าร่วมคอมมูนิตี้ฟรี
            </a>
        </div>

        <!-- FAQ Section -->
        <section class="faq-section">
            <h2 class="faq-title">ทุกคำถามที่คุณอยากรู้เกี่ยวกับ <span class="gradient-text">Domain Trades</span></h2>
            <div id="faq-container"></div>
        </section>
    </div>

    <script>
        /* --- FAQ Data --- */
        const faqData = [
            "ใน Community นี้คุณจะได้รับอะไรบ้าง?",
            "$WEEP Model คืออะไร และผล Backtest เป็นอย่างไร?",
            "ทำไมคุณถึงควรเชื่อใจ Domain Trades ในยุคที่มี Guru ปลอมเต็มไปหมด?",
            "ในเมื่อมีข้อมูลฟรีมากมายบนโลกออนไลน์ ทำไมคุณถึงควรลงทุนกับ Domain Trades?",
            "ต้องใช้เวลาเรียนรู้มากแค่ไหน สามารถเรียนควบคู่ไปกับงานหรือการเรียนได้หรือไม่?",
            "ต้องใช้เวลานานแค่ไหนกว่าจะเริ่มทำกำไรได้?",
            "กลยุทธ์ของ Domain Trades ทำงานอย่างไร?",
            "อะไรทำให้กลยุทธ์ของ Domain Trades แตกต่างจากกลยุทธ์ทั่วไป?",
            "คุณจะรู้ได้อย่างไรว่ากลยุทธ์ของ Domain Trades เหมาะกับคุณ?",
            "คุณจะรู้ได้อย่างไรว่าตัวเองเหมาะกับการเป็น Trader จริง ๆ?",
            "ถ้าครอบครัวหรือคนรอบตัวไม่สนับสนุนเส้นทางการเทรด คุณควรทำอย่างไร?",
            "ใครคือ Coaches ของ Domain Trades?"
        ];

        const faqContainer = document.getElementById('faq-container');
        faqData.forEach((question, i) => {
            const num = (i + 1).toString().padStart(2, '0');
            faqContainer.innerHTML += `
                <div class="faq-item">
                    <div class="faq-header" onclick="toggleFAQ(this)">
                        <div class="faq-number">${num}</div>
                        <div class="faq-question">${question}</div>
                        <div class="faq-indicator">
                            <span style="font-size:0.65rem; font-weight:700; opacity:0.5; letter-spacing:1px;">SHOW VIDEO</span>
                            <svg class="arrow-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                                <polyline points="6 9 12 15 18 9"></polyline>
                            </svg>
                        </div>
                    </div>
                    <div class="faq-content">
                        <div class="faq-video-inner">
                            <div class="video-box">
                                <video controls preload="none"><source src="faq-video-${i+1}.mp4" type="video/mp4"></video>
                            </div>
                        </div>
                    </div>
                </div>
            `;
        });

        function toggleFAQ(header) {
            const item = header.parentElement;
            const isActive = item.classList.contains('active');
            const label = header.querySelector('span');

            document.querySelectorAll('.faq-item').forEach(el => {
                if(el !== item) {
                    el.classList.remove('active');
                    el.querySelector('span').innerText = "SHOW VIDEO";
                    const v = el.querySelector('video'); if(v) v.pause();
                }
            });

            if(!isActive) {
                item.classList.add('active');
                label.innerText = "HIDE VIDEO";
            } else {
                item.classList.remove('active');
                label.innerText = "SHOW VIDEO";
                const v = item.querySelector('video'); if(v) v.pause();
            }
        }

        /* --- Advanced Liquid Parallax Script --- */
        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            
            // 1. เส้นกราฟขยับช้าๆ (Background)
            document.getElementById('trend-layer').style.transform = `translateY(${scrolled * 0.08}px)`;
            
            // 2. แท่งเทียนขยับความเร็วต่างกัน (Depth)
            document.querySelectorAll('.candle').forEach(candle => {
                const depth = candle.dataset.depth;
                candle.style.transform = `translateY(${scrolled * depth}px)`;
            });

            // 3. วิดีโอขยับสวนทางเล็กน้อย
            document.getElementById('main-video-frame').style.transform = `translateY(-${scrolled * 0.02}px)`;
        });

        // Mouse Tilt Effect (Smooth)
        document.addEventListener('mousemove', (e) => {
            const x = (window.innerWidth / 2 - e.pageX) / 100;
            const y = (window.innerHeight / 2 - e.pageY) / 100;
            
            document.getElementById('bg-master').style.transform = `translate3d(${x}px, ${y}px, 0)`;
        });
    </script>
</body>
</html>